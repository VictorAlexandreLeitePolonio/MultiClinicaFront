"use client";
import { formatCurrency } from "@/utils/formatters";
import type { ResultadoFinanceiro } from "../services/relatorios.service";
export function ResultadoCards({ data, loading }: { data?: ResultadoFinanceiro; loading?: boolean }) { if (loading) return <div className="grid gap-4 sm:grid-cols-3">{[1, 2, 3].map((item) => <div key={item} className="h-24 animate-pulse rounded-2xl bg-slate-100" />)}</div>; if (!data) return <div className="rounded-2xl border border-dashed p-6 text-center text-sm text-slate-500">Nenhum resultado disponível.</div>; const cards = [{ label: "Faturamento", value: data.faturamento }, { label: "Despesas", value: data.despesas }, { label: "Resultado", value: data.resultado }]; return <div className="grid gap-4 sm:grid-cols-3">{cards.map((card) => <div key={card.label} className="rounded-2xl border border-[#d7f3ea] bg-white p-5"><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">{card.label}</p><p className="mt-2 text-xl font-bold text-[#0f766e]">{formatCurrency(card.value)}</p></div>)}</div>; }
